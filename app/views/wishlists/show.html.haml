/ %h2= @wishlist.name
/ %h5= @wishlist.description
/ %br
/ / = Wish List Table = /
/ %table.table.table-hover
/ 	%thead
/ 		%tr
/ 			%th Picture
/ 			%th Product Name
/ 			%th Price
/ 			%th In Stock?
/ 			%th Buy Now
/ 	%tbody
/ 		- @wishlist.items.each do |item|
/ 			%tr
/ 				%td= link_to item.name, wishlist_item_path(@wishlist, item)
/				  %td= button_to "Buy Now", class: "btn btn-default"

/ = link_to "Add new item to list", new_wishlist_item_path(wishlist_id: @wishlist.id)


%h1= @wishlist.name
%h5= @wishlist.description

%br

%ul
	- @wishlist.items.each do |item|
		%li= link_to item.name, wishlist_item_path(@wishlist, item)
		%li

%br
= link_to "Add new item to list", new_wishlist_item_path(wishlist_id: @wishlist.id)